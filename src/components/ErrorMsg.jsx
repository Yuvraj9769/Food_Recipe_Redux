import React from "react";
import styles from "./ErrorMsg.module.css";

const ErrorMsg = () => {
  return <h1 className={`${styles["text"]}`}>Sorry Page Not Found 404</h1>;
};

export default ErrorMsg;
